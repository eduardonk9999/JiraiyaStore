import styled from "styled-components";


const Logo = styled.a `
    color: #FFF;
    font-weight: 600;
    font-size: 40px;
    line-height: 150%;
    font-family: var(--font-geist-sans);
`

export default Logo;